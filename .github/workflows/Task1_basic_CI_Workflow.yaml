name: Task1_basic_CI_Workflow
on:
  push:
    branches:
      - main
jobs:
  print_repo_name:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Print repo name
        run: |
          echo "repo name: $GITHUB_REPOSITORY"
